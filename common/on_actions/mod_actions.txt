# This = Country
# From = Species ( Post Modification )
on_modification_complete = {
	if = {
		limit = {
			is_gestalt = yes
		}
		every_owned_leader = {
			limit = {
				is_councilor = yes
				is_ruler = no
			}
			set_gestalt_node_portrait_effect = yes
		}
	}
}
